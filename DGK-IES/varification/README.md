# Verification Artifacts

This directory contains concrete evidence that DGK-IES invariants are enforced.

Included artifacts:

- `sample_state_prev.json` â€“ Baseline state
- `sample_state_curr.json` â€“ Transitioned state
- `state_hash.txt` â€“ SHA-256 output from reference engine
- `attestation.sig` â€“ Signed verification of hash integrity

All artifacts are reproducible using the reference engine.
