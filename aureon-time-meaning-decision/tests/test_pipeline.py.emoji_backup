import json
from engine.tmd_engine import run_pipeline
from validator.tmd_validator import validate_pipeline

def test_basic_pipeline():
    output = run_pipeline("event.test", {"x": 1})
    assert validate_pipeline(output) == True
    print("Pipeline validation: PASSED")

if __name__ == "__main__":
    test_basic_pipeline()
